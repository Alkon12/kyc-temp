type Query
type Mutation

scalar DateTime
scalar Date

type Content {
  key: String
  viewUrl: String
  downloadUrl: String
  thumbnailUrl: String
  previewUrl: String
  metadata: ContentMetadata
}

type ContentMetadata {
  size: Int
  originalFilename: String
}
