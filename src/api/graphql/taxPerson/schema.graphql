type TaxPerson {
  Url: String!
  name: String
  idUser: String!
}

extend type Query {
  scrapingUrl(Url: String!, idUser: String!, saveBool: Boolean!): PersonUpdateEntity!
  personList: [TaxPerson]!
}
