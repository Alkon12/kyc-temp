meta {
  name: Manage Verification Link
  type: graphql
  seq: 3
}

# Validate a token
mutation ValidateToken {
  validateVerificationLink(token: "a1b2c3d4e5f6")
}

# Invalidate a token
mutation InvalidateToken {
  invalidateVerificationLink(token: "a1b2c3d4e5f6")
} 