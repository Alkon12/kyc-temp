meta {
  name: Trips
  type: http
  seq: 1
}

post {
  url: {{host}}/api/v1/cron/jobs/trips
  body: json
  auth: bearer
}

headers {
  X-Environment: LocalPostmanMock
}

auth:bearer {
  token: {{accessToken}}
}
