meta {
  name: BO Auth with Credentials Email-Pass
  type: graphql
  seq: 1
}

post {
  url: {{host}}/api/public/graphql
  body: graphql
  auth: none
}

body:graphql {
  query authWithCredentials($email: String!, $password: String!) {
    authWithCredentials(
      email: $email,
      password: $password,
      provider: "DATABASE"
      ) {
          accessToken
    }
  }
}

body:graphql:vars {
  {
      "email":"backoffice.application.1@grupoautofin.com",
      "password":"1234"
  }
}

tests {
  var jsonData = res.getBody();
  
  
  bru.setEnvVar("accessToken", jsonData.data.authWithCredentials.accessToken);
  
  
  
}
